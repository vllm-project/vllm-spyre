name: Preview docs

on:
  pull_request:
    branches: [ main ]
    paths:
      - "**/*.md"
      - "docs/**"
      - "mkdocs.yaml"

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build_docs.yml
