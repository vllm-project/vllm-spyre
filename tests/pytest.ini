[pytest]
markers =
    cpu: test running on CPU
    cb: continuous batching
    decoder: test decoder models
    embedding: test embedding models
    e2e: End-to-end test
    utils: utility tests
    basic: Basic correctness tests
    compat: backward compatibility tests
